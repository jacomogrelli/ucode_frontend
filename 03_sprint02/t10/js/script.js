"use strict"

let makeHeroName = () => {
  let animal, gender, age, name;

  animal = prompt("What animal is the superhero most similar to?\nInput \
  usage: Max 20 symbols, only one word, only letters");
  console.log(animal);


}

makeHeroName();
